# expense-tracker-monorepo

Scaffolded monorepo (pnpm) with small React client + Serverless TypeScript backend (lambda health endpoint).

**Do not commit secrets.** See packages/\*/.env.example for env var examples.
MD
